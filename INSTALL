# INSTALL

## On ubuntu

```
apt install build-essential ruby-all
git clone <THISREPO> ghpages
cd ghpages
bundler config set --local path 'vendor/bundle'
bundle install
bundle update
bundle exec jekyll serve
```
